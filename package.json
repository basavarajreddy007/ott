{
  "name": "stream-app",
  "version": "1.0.0",
  "description": "Fullstack streaming app",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist backend/dist",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
